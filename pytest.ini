[pytest]
asyncio_mode = auto
asyncio_default_fixture_loop_scope = function
markers =
    unit: fast offline tests (encoding, models, helpers)
    set_params: tests that exercise property writes
    get_params: tests that exercise property reads
    auth: tests that exercise login lifecycle / auth recovery
    live: marks a test as requiring network access to the Napoleon/Ayla cloud
